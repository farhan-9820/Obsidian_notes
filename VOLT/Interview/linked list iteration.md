```python

def iteration(head):
    # Line 2: Start at the head of the list
    temp = head
    
    # Line 3: Continue as long as temp is not None (NULL)
    while temp is not None:
        # Line 4: Access and print the data (using .elem from your previous class)
        print(temp.elem)
        
        # Line 5: Move to the next node in the sequence
        temp = temp.next
```

