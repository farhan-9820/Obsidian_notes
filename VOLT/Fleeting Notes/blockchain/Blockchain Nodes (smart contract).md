A **Node** is simply any computer running the blockchain client software. When the video mentions "creating" nodes, it usually means setting up isolated directories (data folders) on a machine to represent different participants in the network. Each node will have its own:

- **Private/Public Keypairs:** To sign transactions and identify itself.
    
- **Local Database:** To store its own copy of the blockchain ledger.